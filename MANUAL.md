# 直接对项目 Agent 说

尚未安装时，把 Bootstrap 的 GitHub 链接发给当前项目 Agent：

> 请在当前项目初始化这个 Bootstrap，仅本地生效，不进入 Git。保留原有规则，完成后告诉我怎么使用。

安装、依赖准备和校验都由 Agent 完成。安装后在同一个对话框继续：

1. **了解项目**：“告诉我这个项目能做什么，并打开项目地图。”
2. **提出修改**：“修改登录失败提示，让用户知道如何重试。”
3. **限定范围**：“只修改 NODE:X；需要涉及其他节点时先停下说明。”让 Agent 从地图确认真实节点 ID。
4. **查看结果**：“告诉我完成了什么、如何验证。”默认独立 Review 通过后排队合并；要亲自合并，补充 `require human merge`。
5. **退出**：“移除当前项目的 Bootstrap，保留我的任务改动。”Agent 展示清理范围，等你确认再删除；需要保留的本地规则先让它备份。

默认 **Local-only**：Bootstrap 留在本机，不进入项目提交。若想让规范随项目提交，安装时明确说“使用 Standard”。

默认 **Local-first**：先看本地效果。要发布生产，明确说“将当前版本部署到生产”。
只有明确说“长期使用 Production-direct”才授予持续生产部署权限；必要测试、Build 和部署检查始终执行。

Agent 只做完成当前需求的必要修改；Review 只判断、不改代码。涉及扩大边界、规则冲突或缺少权限时，它会说明具体问题。
新对话通过本地规则入口加载；若客户端没有读取入口，让它“读取本地 Bootstrap 规则并检查安装”。
地图用于查看，对话框是操作入口，无需手动编辑配置或运行命令。

下一步（1 分钟）：对当前项目 Agent 说“告诉我这个项目能做什么”。
